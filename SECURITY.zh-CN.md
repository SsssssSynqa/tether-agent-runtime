# 安全策略

English documentation: [SECURITY.md](SECURITY.md).

## 报告漏洞

如果你怀疑存在漏洞、凭据泄露、私密对话外泄、授权绕过或隐私暴露，**请不要开公开 issue**。

请用仓库的 **Security** 标签页提交一份私有的 GitHub Security Advisory，并包含：

- 受影响的组件与版本；
- 一个最小的、合成的复现；
- 预期行为与实际行为；
- 影响范围与触发前提；
- 任何建议的修复方案；
- 是否可能已经暴露了真实凭据或个人数据。

**不要**附上真实对话、token、用户标识、通道导出、记忆数据库或生产日志，请用合成值替换。维护者会通过该 advisory 确认收到，并在那里协调修复与披露。

## 受支持的版本

在第一个稳定版发布之前，安全修复针对默认分支上的最新版本。开始有版本化发布之后，本节会说明受支持的发布线。

## 安全边界

Tether 协调的是几个信任边界不同的组件：

- 通道适配器负责认证入站，并塑造接收方可见的输出；
- 运行时持有权威会话与因果日志；
- 供应商适配器把选中的上下文发给配置好的模型服务；
- 记忆文件夹里装着敏感的原始与派生记录；
- Tether Console 读取本地记忆，默认绑定 loopback；
- 按轮次的能力授权工具与写入，而**不分叉身份上下文**。

Selfsame Protocol 保护的是连续性，**它不是一个认证或加密协议**。运营者必须为自己的部署配置相称的访问控制。

## 部署检查清单

- 密钥存放在仓库之外、也在被提交的配置之外。
- 远程供应商端点必须用 HTTPS。仅对 `localhost`、`127.0.0.0/8`、`::1` 允许 HTTP，并且永远不要把凭据嵌进供应商 URL 的 userinfo 或查询参数里。
- 自定义的 Telegram API base 适用同一条 HTTPS-或-loopback 边界；拒绝 URL 里的凭据、查询参数和 fragment。
- 认证不要放在普通的供应商 `headers` 里；用 `apiKeyEnv` 或 `headerEnv`，让凭据值来自进程环境。
- 运行时数据放在源码目录之外，并使用最小权限的文件系统权限。
- 每个工具 workspace 都要与 `storage.root` 物理分离；Tether 会拒绝两个方向的包含关系，运营者仍应检查 mount 与符号链接布局。
- 按通道审查工具策略。没有经过具体测试的需要时，群工具保持 deny；不可信写入要求 approval。
- 除非有意配置了一层独立认证的反向代理，否则把 Console 与管理端点绑定在 loopback 上。
- 把原始记录、订正、卡片、向量、日志和备份都当作敏感数据对待。
- 把内置备份目录视为未加密明文；外部 workspace/附件根另设备份策略。
- 在发送真实内容之前，先确认供应商与通道的数据留存策略。
- 一旦某个密钥出现在终端记录、日志、issue 或 commit 里，**立即轮换**。
- 能力策略要与上下文连续性分开审查。
- 发布分支或发行归档之前，先跑 `scripts/check-public-snapshot`。
- 备份原始权威，并测试恢复流程——**测试时不要创建一个替代的人格会话**。
- 宿主机 service manager 应指向 `tether-supervisor`，不能再起第二个子 runtime；宿主机重启策略也要有界，让 crash-loop 耗尽保持可见。
- 迁移、备份、恢复、记忆重建/回填或死信修改前，先停掉 supervisor 与 runtime；绝不能删 lock 绕过离线边界。

## 不在公开测试范围内

不要针对你不拥有、或未获授权评估的账号、bot、端点或部署做测试。不要提交拒绝服务流量、社会工程、撞库，或任何真实的私人数据。
