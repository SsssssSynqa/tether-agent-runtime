# 隐私模型

English documentation: [PRIVACY.md](PRIVACY.md).

Tether 是 local-first 的：权威历史与记忆存放在运营者自己控制的文件夹里，Tether Console 在本地读取这些文件夹。**Local-first 是一种部署属性，不是"配置好的供应商和通道永远收不到数据"的承诺。**

## 数据流向

| 组件 | 它可能收到的数据 | 主要控制手段 |
|---|---|---|
| 通道适配器 | 进出通道的消息、投递元数据 | 通道配置与访问策略 |
| Agent 运行时 | 权威会话、因果 ID、活跃上下文、输出 | 本地数据目录与文件系统权限 |
| 模型供应商 | 送去推理的 prompt、选中的历史、附件与工具结果 | 供应商适配器与供应商账号策略 |
| 记忆管线 | 原始记录，以及派生的卡片、断言、事件、订正与索引 | 本地记忆根目录与留存策略 |
| Tether Console | 已配置本地记忆文件夹的只读视图 | 绑定 loopback 与本地进程访问控制 |
| 备份 | 被纳入的那部分运行时与记忆数据 | 运营者控制的目标位置与加密 |

## 这个仓库里不包含什么

公开仓库里**绝不能**含有生产对话、账号或聊天标识、凭据、部署地址、私有端点、用户档案、记忆数据库、运行日志、下载的媒体或备份归档。所有示例与测试都必须是合成的。

发布之前先跑 `scripts/check-public-snapshot`。这道门能发现常见的误包含，但它无法理解每一条私密事实——**人工复核仍然是必需的。**

## 外部服务

当运营者启用了某个供应商或通道，数据就按那个外部服务自己的条款与留存策略被处理。Tether **无法**抹掉外部服务留存的副本。请审阅那些策略，并在合适的时候尽量减少送出去的上下文和附件——但不要为此造出第二份人格历史。

Tether 自身不需要任何托管的分析或遥测。如果下游的分发版本加入了遥测，应当显著披露，并取得其用户所需的任何同意。

## 本地存储

原始权威被刻意设计成只追加的，因为身份连续性依赖于保留证据。这也意味着下面这几件事更重要了：

- 明确把存储位置放在源码目录之外；
- 文件系统权限遵循最小权限原则；
- 采用与设备和威胁模型相称的加密；
- 制定留存与备份策略；
- 谨慎处理派生出来的向量与摘要，它们仍可能透露来源内容；
- 当确实需要删除时，走经过认证、可审计的合法删除流程。

人工订正**不会**抹掉原始证据，它只是追加一份取代性的解读。在开始收集敏感内容之前，运营者应当把这个行为讲清楚。

## Console 的暴露面

Console 后端默认绑定 `127.0.0.1`，其设计定位是一个对本地记忆的只读浏览器。不要把它直接暴露到公网。如果确实需要远程访问，请把它放在一层经过认证、加密的访问层之后，并独立测试授权是否真的生效。

## 日志与诊断

日志里应当只有事件类别和不透明的因果 ID，不应有消息正文、token、认证 header 或完整的供应商响应。在分享诊断信息之前，逐行检查，把真实的标识和内容替换成合成占位符。

## 请求与事故

部署出去的数据由运营者控制，不由上游源码仓库控制。Tether 自身的隐私问题，请按 [SECURITY.zh-CN.md](SECURITY.zh-CN.md) 私下报告。
