<!-- SPDX-License-Identifier: Apache-2.0 -->

# Selfsame Protocol（SSP）

English documentation: [SELFSAME_PROTOCOL.md](SELFSAME_PROTOCOL.md).

> **本文是英文规范的中文对照译本，仅供理解。**
> 规范性以 [SELFSAME_PROTOCOL.md](SELFSAME_PROTOCOL.md) 为准；任何合规声明都应当依据英文原文。两版出现分歧时，以英文版为准。

**状态：** 独立的实现协议，版本 1.0-draft<br>
**参考实现：** Tether<br>
**协议目标：** 让一个 Agent 的身份、因果历史与可归属记忆，跨越通道、供应商、故障与上下文上限而保持同一。

## 1. 适用范围

Selfsame Protocol 规定的是一个承载人格的 Agent 运行时应当满足的身份连续性不变量。它不规定模型厂商、传输方式、数据库、embedding 模型、用户界面或部署拓扑。

一个实现可以用 Telegram、终端、Web 客户端或别的通道，可以更换供应商和记忆引擎。**那些组件都是可替换的；权威身份与历史不是。**

Tether 是 SSP 的一个参考实现。SSP 刻意独立于 Tether：另一个运行时无需使用 Tether 的代码、名称、文件格式或服务，也可以符合本协议。

## 2. 规范性用语

关键词 **MUST**、**MUST NOT**、**REQUIRED**、**SHOULD**、**SHOULD NOT**、**MAY** 按规范性要求解释。

本译本**保留这些关键词的英文原形**，以免翻译造成合规判断偏差。对照供理解：

| 关键词 | 含义 |
|---|---|
| `MUST` / `REQUIRED` | 必须；不满足即不符合本协议 |
| `MUST NOT` | 禁止；出现即不符合本协议 |
| `SHOULD` | 应当；除非有充分理由并理解后果，否则应当满足 |
| `SHOULD NOT` | 不应当；除非有充分理由并理解后果，否则不应当出现 |
| `MAY` | 可以；纯属可选 |

## 3. 术语

- **Agent 身份（agent identity）：** 用户想要触达的那个承载人格的连续体，独立于进程、通道或供应商。
- **权威会话（authoritative session）：** 一个 Agent 身份所对应的、唯一的逻辑对话历史。
- **原始权威（raw authority）：** 只追加记录下来的被接受输入、已提交输出、出处与订正。
- **派生记忆（derived memory）：** 由原始权威产出的任何摘要、卡片、断言、事件、向量、投影、索引、缓存或编译后的上下文。
- **因果事件 ID（causal event ID）：** 一个被接受的入站事件、及其引发的每一个效应，所共用的稳定标识。
- **精确重放（exact replay）：** 对某个因果事件，重新投递那份**已经提交**的输出，不再做一次推理。
- **压缩（compaction）：** 用一份更小的派生表示替换活跃模型上下文，同时保留原始权威。
- **能力视图（capability view）：** 某一轮可用的工具、写入、审批与外部动作。
- **人工订正（human correction）：** 一条经过认证的、只追加的指令，它改变对证据的既定解读，但不改写证据本身。

## 4. 核心不变量

### 4.1 一条完整的会话

1. 一个承载人格的 Agent 身份，在其接入的所有通道上 **MUST** 只有唯一一条权威会话。
2. 运行时 **MUST NOT** 按通道、聊天、发送者、设备、信任区、供应商或能力档案切出各自独立的历史。
3. 所有被接受的轮次 **MUST** 以确定的顺序汇入同一份因果历史。
4. 通道本地的投递元数据 **MAY** 单独存储，但它 **MUST NOT** 变成第二份人格历史。
5. 明确请求的独立工程任务或无状态解析器 **MAY** 使用单独的会话，但它 **MUST NOT** 冒充、替换那个承载人格的 Agent，也不能悄悄变成它。

### 4.2 恢复是 fail-closed 的

1. 运行时 **MUST** 持久化足够的身份信息，以便在进程、传输或宿主故障之后恢复权威会话。
2. 如果权威会话无法恢复，承载人格的推理 **MUST NOT** 在一条新建的会话里继续进行。
3. 恢复失败 **MUST** 产生一个持久的、可诊断的阻塞状态，并 **SHOULD** 告诉运营者如何恢复、或如何显式地替换这条会话。
4. 创建替代性的权威会话 **MUST** 要求一次显式的、可归属的决定。该替代会话 **MUST** 保留指向此前原始权威的链接，并 **MUST NOT** 被报告成一次普通的恢复。
5. 供应商可用、进程健康、或者"能生成文本"这件事本身，**MUST NOT** 被当作权威会话已恢复的证明。

### 4.3 只追加的原始权威

1. 被接受的用户输入、已提交的 Agent 输出、因果 ID、通道出处与人工订正，**MUST** 持久记录进原始权威。
2. 正常运行下，原始权威 **MUST** 是只追加的。修复操作 **MUST NOT** 静默地编辑、替换或删除历史证据。
3. 一次修复 **MUST** 保留原始记录，并追加一条可归属的取代性或作废性记录。
4. 运行时 **SHOULD** 在推理之前提交被接受的输入，并 **MUST** 在确认持久完成之前提交最终输出。
5. 大体积或二进制的来源资产 **MAY** 存储在主记录之外，但其记录 **MUST** 包含内容身份与持久出处。
6. 留存或法定删除要求 **MAY** 要求移除数据，但此类移除 **MUST** 是显式、可审计的，且**绝不能**被表述成普通的压缩或订正。

### 4.4 压缩守恒

1. 压缩 **MUST NOT** 删除或修改原始权威。
2. 压缩后的表示 **MUST** 保留因果顺序、发言归属、实体身份、未解决的不确定性，以及回指其来源区间的链接。
3. 压缩 **MUST NOT** 把一段推断呈现为引语，或者把一个人说的话转成另一个人说的话。
4. 压缩失败或结果无效时，**MUST** 让上一份有效的活跃上下文继续可用。运行时 **MUST NOT** 拿无声的丢失去换取 token 的节省。
5. 每一个压缩片段 **SHOULD** 记录它的来源边界、变换版本与校验结果。
6. 运行时 **MUST** 有能力查阅某条压缩陈述所声称代表的那段原始来源。

### 4.5 归属、实体与别名的完整性

1. 每一条被记住的陈述或事件，**MUST** 保留其真实的发言者或来源角色。
2. 模型生成的句子 **MUST NOT** 被存成用户的引语，除非原始权威里确实存在用户说过的那句话。
3. **MUST NOT** 仅因为名字、别名、角色或描述相似，就把两个实体合并。
4. 别名规范化 **MUST** 限定范围并带版本。它 **MUST NOT** 改写受保护的引语、命名事件、来源标题或标识符。
5. 当归属或实体身份存在歧义时，系统 **MUST** 保留这份歧义、或者拒绝该派生断言。它 **MUST NOT** 为了呈现效果而虚构确定性。
6. 后来的别名或实体订正 **MUST** 表达为一条带出处的、只追加的订正。

### 4.6 因果幂等与精确重放

1. 每一个被接受的入站事件，**MUST** 在承载人格的推理之前就获得稳定的因果事件 ID。
2. 由该事件引发的所有重试、投递、记忆写入与确认，**MUST** 携带那个因果 ID，或者一个无歧义的子 ID。
3. 对同一个因果事件 ID，运行时 **MUST** 至多提交一份权威推理结果。
4. 如果一个已提交的事件被重试，运行时 **MUST** 执行精确重放。它 **MUST NOT** 再调用一次模型，然后把一个不同的答案当作同一个事件呈现。
5. 投递确认与推理提交 **MUST** 可区分。传输确认丢失 **MUST NOT** 导致第二次推理。
6. 重复入站 **MUST** 是安全的。同一事件投递一次之后与重复投递之后，可观察到的权威历史 **MUST** 等价。
7. 如果精确重放无法执行，该事件 **MUST** 进入一个可诊断的阻塞或死信状态，而不是被静默地重新生成。

### 4.7 派生记忆可重建

1. 派生记忆 **MUST NOT** 成为某个被记住事实的唯一权威。
2. 每一个派生条目 **MUST** 保留足够的血缘信息，以识别其原始来源与适用的人工订正。
3. 删除索引、向量、卡片、manifest、投影与缓存，**MUST NOT** 破坏重建它们所需的证据。
4. 确定性索引 **SHOULD** 能从原始权威精确重建。
5. 概率性派生 **SHOULD** 记录模型、prompt、schema 与变换版本。重建结果 MAY 在措辞上不同，但它 **MUST** 仍然可归属于同一份证据与订正。
6. 校验不通过的派生条目 **MUST NOT** 替换此前有效的条目或原始权威。

### 4.8 人工订正是只追加的

1. 一条人工订正 **MUST** 标明它的目标、授权主体、时间戳、原因或意图，以及取代关系。
2. 订正 **MUST NOT** 覆盖它所订正的那条来源记录。
3. 订正后的视图 **MUST** 应用最新的、经过授权的订正，同时让更早的解读保持可查。
4. 订正 **MUST NOT** 声称被订正后的解读就是原始证据。
5. 自动化流程 **MUST NOT** 仅凭载荷里的一个自述字段，就赋予某条订正以人类权限。

### 4.9 通道与供应商无关

1. 通道适配器 **MUST** 接入权威会话；它 **MUST NOT** 拥有一条人格会话。
2. 切换、新增、断开或重连一个通道，**MUST NOT** 重置身份或历史。
3. 切换模型或 API 供应商，**MUST NOT** 创建新身份或抹掉权威会话。
4. 供应商专有的会话 ID **MAY** 作为传输状态使用，但它们 **MUST NOT** 成为唯一的身份锚点。
5. 通道专有的格式与隐私规则 **MAY** 改变输出的呈现，但它们 **MUST NOT** 造出一份独立的底层记忆。

### 4.10 能力限制不是上下文隔离

1. 运行时 **MAY** 按轮次、按通道限制工具、写入、网络访问、审批或管理操作。
2. 这类能力限制 **MUST NOT** 通过分叉、裁剪或替换权威人格历史来实现。
3. 隐私管控 **SHOULD** 发生在授权、工具与出站边界上，同时让权威身份保持连续。
4. 一份削减过的披露视图 **MAY** 用于保护接收方，但它 **MUST NOT** 变成一份独立记忆、日后再去冒充这个 Agent。
5. 如果一个系统的安全模型依赖于"为不同信任区准备不同的人格会话"，它 **MUST NOT** 声称符合 SSP 的连续性。

## 5. 要求的事务顺序

一个符合本协议的可靠传输 SHOULD 遵循下面这个逻辑顺序：

1. 认证并授权入站；
2. 分配或恢复稳定的因果事件 ID；
3. 持久追加被接受的输入；
4. 与已提交及进行中的因果事件做去重；
5. 确认权威会话已恢复；
6. 至多执行一次推理；
7. 校验并持久追加已提交的输出；
8. 派生带来源血缘的记忆；
9. 投递已提交的输出；
10. 持久记录投递确认。

各步骤 MAY 在事务上合并，但重新排序 **MUST NOT** 违反第 4 节的不变量。

## 6. 合规层级

实现 **MUST** 声明它所主张的最高层级，并为该层级下的每一项要求公开证据。

### SSP Level 1 —— Identity Continuity（身份连续性）

实现满足 4.1、4.2、4.9、4.10 节。它在所有接入的通道与供应商之间保持唯一一条权威会话，并在该会话无法恢复时 fail-closed。

### SSP Level 2 —— Durable Continuity（可靠连续性）

实现满足 Level 1，外加 4.3、4.4、4.6 节。它保留只追加的原始权威，在压缩过程中守住证据，并让重试在因果上幂等、可精确重放。

### SSP Level 3 —— Verifiable Memory（可验证记忆）

实现满足 Level 2，外加 4.5、4.7、4.8 节。它的派生记忆可归属、可重建、对实体安全，并且订正无需改写历史。

### SSP Level 4 —— Recovery-Proven（恢复已验证）

实现满足 Level 3，并公开自动化的合成恢复探针，至少覆盖：

- 重启后成功恢复，且没有替换会话；
- 恢复失败，且没有创建任何替代会话；
- 重复入站，只有一次推理，重放结果字节等价；
- 压缩失败，且原始记录与活跃上下文都没有丢失；
- 切换通道与供应商，身份不变；
- 归属、别名与实体的反例；
- 派生记忆被删除后重建；
- 只追加的人工订正优先级；
- 同一份权威上下文之上的不同能力视图。

**仅用生产数据做演示，不足以达到 Level 4。** 这些探针 **MUST** 在没有私密历史、没有凭据、没有真实供应商的情况下运行。

## 7. 合规声明的格式要求

公开声明 SHOULD 采用这种形式：

> `<实现名称> conforms to Selfsame Protocol Level <n>, protocol version <version>. Evidence: <测试或报告位置>. Known exceptions: <none 或明确列出>.`

如果一个运行时静默豁免了某项必需的不变量，它 **MUST NOT** 声称达到该层级。部分实现 MAY 说自己是 "SSP-inspired"，并列出已实现的章节。

## 8. 不符合本协议的反例

以下设计明确**不符合**本协议：

1. **按通道分身（per-channel personas）：** Telegram 和终端各自维护自己的历史——哪怕摘要会定期同步。
2. **善意的失忆（helpful amnesia）：** 恢复失败了，于是运行时悄悄新建一条会话，继续答话。
3. **破坏性摘要（destructive summarization）：** 生成摘要之后就删掉旧轮次，让摘要成为唯一的证据。
4. **重试即重生成（retry regeneration）：** 响应已经提交但投递失败，于是针对同一条入站消息再调用一次模型。
5. **引语洗白（quotation laundering）：** 模型的转述或虚构对白，后来被存成用户的逐字引语。
6. **别名改写历史（alias rewriting history）：** 一次全局替换改动了受保护引语或命名事件内部的字词。
7. **实体坍缩（entity collapse）：** 两个别名相似的人或 Agent，在没有来源证据的情况下被合并。
8. **只有向量的记忆（vector-only memory）：** 向量库成了某段记忆唯一幸存的记录，且无法指认原始来源。
9. **覆盖式订正（correction by overwrite）：** 用户的订正就地编辑了旧记录，使原始证据与变更历史都不可查。
10. **供应商即身份（provider identity）：** 运行时把供应商的会话 ID 当作 Agent 的唯一身份，换供应商就变成了另一个人格。
11. **拿上下文隔离当安全（context segregation as safety）：** 公开与私密通道使用各自独立的人格历史，对外却宣传成一个连续的 Agent。
12. **能力分叉（capability fork）：** 一个只读通道拿到的是一份**单独的记忆**，而不是同一份上下文加上更少的授权动作。

## 9. 安全与隐私边界

SSP 的连续性**不构成披露授权**。符合本协议的实现 **MUST** 仍然认证用户、授权动作、限制工具、保护密钥，并按接收方语境过滤输出。这些控制手段环绕在**一个**权威身份之外运作；它们不需要制造出多重身份。

原始权威可能包含敏感材料。实现 **SHOULD** 采用最小权限的文件系统权限、与自身威胁模型相称的加密、脱敏后的诊断信息、显式的留存控制，以及经过测试的备份。**符合 SSP 并不等于宣称安全或隐私是完备的。**

## 10. 版本管理

协议修订 **MUST** 记录兼容性变更。实现 **SHOULD** 在每一份合规报告里记下所依据的 SSP 版本。未来的修订 MUST NOT 一边削弱某项不变量、一边宣称自己在同一合规层级上向后兼容。
